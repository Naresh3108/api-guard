[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -v --tb=short --strict-markers

markers =
    smoke: basic health checks
    regression: all tests
    contract: response structure tests
    negative: error and bad input tests
    performance: speed tests
    auth: login and token tests